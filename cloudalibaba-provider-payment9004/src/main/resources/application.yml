server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: http://192.168.15.129:8888
#        server-addr: localhost:8848 #配置Nacos地址 // 本机启动的nacos-
management:
  endpoints:
    web:
      exposure:
        include: '*'
